import Loading from "../product/[productId]/loading";

export default function LoadingPage() {
  // You can add any UI inside Loading, including a Skeleton.
  return (
    <div>
      <Loading />
    </div>
  );
}
